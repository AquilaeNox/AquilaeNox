## Fábio Cruz

Over twenty years in IT, working with networks and systems on infrastructure,
identity, and the servers underneath both.

I'm learning **Go** and **C** to build the tooling those environments need
instead of working around its absence.

---

### What I do

I design and maintain systems and networks that are secure and reliable.

The experience is from the field rather than from a lab: production estates
across multiple sites, the Windows infrastructure that depends on them, and the
failure modes that only appear when something real is relying on it. MikroTik
is home ground, though the principles travel across vendors. On premises and
in Azure.

I also audit networks I did not build, and write the plan that fixes them.

---

### Stack

`Switching` · `Routing` · `Firewalling` · `VPN` · `Authentication` ·
`Virtualisation` · `Cloud` · `Identity` · `Systems`
